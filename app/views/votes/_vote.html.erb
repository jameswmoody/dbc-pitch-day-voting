<%= form_tag('/votes', method: :post, id: "pitch#{vote_pitch.id}") do %>
	<%= render 'pitches/pitch', pitch: vote_pitch %>
	<%= check_box_tag(:hide) %>
	<%= label_tag(:hide, "Hide") %>
	<%= submit_tag("Up") %>
	<%= submit_tag("Down") %>
<% end %>